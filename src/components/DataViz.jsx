/* Placeholder — replace with actual data visualization component */
export default function DataViz() {
  return (
    <div
      aria-label="Data visualization — placeholder"
      className="w-full aspect-video border border-dashed border-[var(--border)] flex items-center justify-center mt-16"
    >
      <span className="font-mono text-xs text-[var(--muted)] tracking-widest">
        [ DataViz ]
      </span>
    </div>
  )
}
